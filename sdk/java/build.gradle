plugins {
    id 'java'
    id 'java-library'
    id 'maven-publish'
    id 'signing'
}

ext {
    PUBLISH_GROUP_ID = 'ai.picovoice'
    PUBLISH_VERSION = '2.0.0'
    PUBLISH_ARTIFACT_ID = 'picovoice-java'
}

repositories {
    mavenCentral()
}

sourceSets {
    main {
        java {
            srcDirs = ['src']
        }
    }
    test {
        java {
            srcDirs = ['test']
        }
    }
}

javadoc.options.addStringOption('Xdoclint:none', '-quiet')

if (file("${rootDir}/publish-mavencentral.gradle").exists()) {
    apply from: "${rootDir}/publish-mavencentral.gradle"
}

dependencies {
//    implementation 'ai.picovoice:porcupine-java:2.0.0'
//    implementation 'ai.picovoice:rhino-java:2.0.0'
    implementation files('../../resources/rhino/binding/java/build/libs/Rhino.jar')
    implementation files('../../resources/porcupine/binding/java/build/libs/Porcupine.jar')
    testImplementation 'org.junit.jupiter:junit-jupiter:5.4.2'
}

test {
    systemProperty 'pvTestingAccessKey', System.getProperty('pvTestingAccessKey')
    useJUnitPlatform()
}

jar {
    from {configurations.runtimeClasspath.collect { it.isDirectory() ? it : zipTree(it) } }
}