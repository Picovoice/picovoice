name: picovoice_flutter
description: A Flutter package for Picovoice's end-to-end voice platform.
version: 2.0.0
homepage: https://picovoice.ai/

environment:
  sdk: ">=2.12.0 <3.0.0"
  flutter: ">=1.20.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_voice_processor: ^1.0.6
  porcupine_flutter:
    path: ../../resources/porcupine/binding/flutter
  rhino_flutter:
    path: ../../resources/rhino/binding/flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  lints: ^1.0.1

flutter:
