context:
  expressions:
    setAlarm:
      - "set (an, the) alarm for $pv.TwoDigitInteger:hours hours (and) $pv.TwoDigitInteger:minutes minutes (and) $pv.TwoDigitInteger:seconds seconds"
      - "set (an, the) alarm for $pv.TwoDigitInteger:hours hours (and) $pv.TwoDigitInteger:minutes minutes"
      - "set (an, the) alarm for $pv.TwoDigitInteger:hours hours (and) $pv.TwoDigitInteger:seconds seconds"
      - "set (an, the) alarm for $pv.TwoDigitInteger:hours hours"
      - "set (an, the) alarm for $pv.TwoDigitInteger:minutes minutes (and) $pv.TwoDigitInteger:seconds seconds"
      - "set (an, the) alarm for $pv.TwoDigitInteger:minutes minutes"
      - "set (an, the) alarm for $pv.TwoDigitInteger:seconds seconds"
      - "$pv.TwoDigitInteger:hours hours (and) $pv.TwoDigitInteger:minutes minutes (and) $pv.TwoDigitInteger:seconds seconds"
      - "$pv.TwoDigitInteger:hours hours (and) $pv.TwoDigitInteger:minutes minutes"
      - "$pv.TwoDigitInteger:hours hours (and) $pv.TwoDigitInteger:seconds seconds"
      - "$pv.TwoDigitInteger:hours hours"
      - "$pv.TwoDigitInteger:minutes minutes (and) $pv.TwoDigitInteger:seconds seconds"
      - "$pv.TwoDigitInteger:minutes minutes"
      - "$pv.TwoDigitInteger:seconds seconds"
    reset:
      - "reset"
  slots: